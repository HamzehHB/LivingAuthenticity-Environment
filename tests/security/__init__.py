"""Repository-wide verification suite (scans the whole repository root).

The scan roots below are anchored to the repository root, not to this
directory, so this test continues to cover the entire project after the
test-tree reorganization.
"""
